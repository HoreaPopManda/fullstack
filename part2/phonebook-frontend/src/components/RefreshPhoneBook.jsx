const RefreshPhoneBook = ({refreshPhoneBook }) => {
  return (
    <div>
        <button onClick={refreshPhoneBook}>refresh the phonebook</button>
    </div>
  );
};

export default RefreshPhoneBook;
