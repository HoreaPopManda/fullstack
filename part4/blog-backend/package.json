{
  "name": "blog-backend",
  "version": "1.0.0",
  "description": "Part 4 fullstack course, blog project",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development node --watch index.js",
    "dev:testdb": "cross-env NODE_ENV=test node --watch index.js",
    "test": "cross-env  NODE_ENV=test node --test",
    "build:ui": "rm -rf dist && cd ../blog-frontend && npm run build && cp -r dist ../blog-backend/dist",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "author": "Horea POP MANDA",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.6",
    "cross-env": "^10.1.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "lodash": "^4.18.1",
    "mongoose": "^9.6.2"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "eslint": "^10.4.0",
    "globals": "^17.6.0",
    "supertest": "^7.2.2"
  }
}
